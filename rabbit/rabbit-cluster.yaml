apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: rabbit-cluster
  region: eu-west-3
  version: "1.34"

accessConfig:
  authenticationMode: API_AND_CONFIG_MAP

managedNodeGroups:
  - name: rabbit-workers-2
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    volumeSize: 20